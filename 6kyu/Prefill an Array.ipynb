def prefill(n,v='undefinded'):
    from itertools import repeat
    result = []
    
    if n is None:
        raise TypeError("None is invalid")
    
    try:
        n = int(n)
        result.extend(repeat(v,int(n)))
    
    except ValueError:
        raise TypeError(n + " is invalid")
    
    return result
